import type { Block } from "payload"

export const LiveWebsiteTestingSites = {
  slug: 'liveWebsiteTestingSites',
  admin: {
    disableBlockName: true,
  },
  fields: [
    
  ]
} as const satisfies Block
