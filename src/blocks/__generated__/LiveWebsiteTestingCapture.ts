import type { Block } from "payload"

export const LiveWebsiteTestingCapture = {
  slug: 'liveWebsiteTestingCapture',
  admin: {
    disableBlockName: true,
  },
  fields: [
    
  ]
} as const satisfies Block
